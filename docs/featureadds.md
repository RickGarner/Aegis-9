* For the even stream activity/alerts pane, when an alert is clicked on, a separate window should open with details about each service that is being alerted on. Along with each service being reported on an individual line, service details should include Service name, current state, date issue first occurred, details on why the service did not start as expected if those details are available, possible solutions that can fix the issue, a fix button that when pressed, automatically tries to fix the issue and an acknoweldment button that will dismiss the alert for 24 hours from the main UI, unless the UI is restarted.
* Create a document library that can scan an existing library, making those documents available for review/edit, directly through another Aegis window. New documents can also be upload, edited and deleted. Docs to be uploaded, edited or deleted require final supervisor approval before the actual action is performed. New document creation, is allowed without approval, but final submission requies supervisor approval. When the user creates a create new document button, a new windows should open that contain a place to enter the document title, a brief discrption of the document an upload box to upload any documents that might be needed. Uploaded docs should be stored. There should also be an editor area that the user can start creating the document. It should look like a typical document editor with typical document editor features. The menu bar should be like a typical editor menu bar. Saving should allow the option of saving as a .txt, .csv, .md, .rtf doc type. When the user first clicks the create a new document button, the user should have the option of creating the document manually or having Aegis create it by examining uploaded documents. Aegis generated documents require user review, approval and then supervisor approval. Aegis should create the document within the editor windows.
* **Approved/planned — Aegis Developer Studio:** add a native Aegis slide-out panel that will launch and supervise the separate Code - OSS-based IDE currently developed as WolfForge. It will support local, repository-aware coding assistance; PowerShell creation; C#/VB.NET conversion; solution/project creation; governed file operations; and retained build/test evidence. Generated, converted, or user-submitted software will use the applicable user, BSOC developer, and supervisor approval gates. The approved architecture, existing foundation, privacy boundary, and delivery milestones are tracked in `docs/AEGIS-DEVELOPER-STUDIO.md`.
* 

