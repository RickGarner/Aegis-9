# Aegis-9 local publish — 2026-09-24

Destination: `D:\Program Files\GDIT\Aegis\Aegis-9`  
Executable: `Aegis.Desktop.exe`  
Source: current working tree on `feature/workflow-automation-monitoring-2026-08-31`.
Includes uncommitted Workshop integration and other existing working-tree changes.

## Package

- Release, framework-dependent .NET 8 WPF desktop; single-file publishing disabled.
- Backend application code and configuration alongside the executable.
- Bundled Python 3.11.9 and dependencies copied from the validated local environment.
- Avatar/WebView assets, local appsettings and .env, policy files, and tool parity contract.
- Workshop setup, integration tracker, manual test plan, and synthetic validation script.
- About 0.43 GB / 9,293 payload files before this publish record was added.

The target folder did not previously exist. Repository databases, uploaded
files, generated workflows, logs, and history were not migrated or modified.
The published application has its own installation-relative runtime storage;
existing per-user desktop preferences remain shared through LocalAppData.
Workshop/model and voice services are external workstation dependencies.
The installed .NET 8 Windows Desktop runtime is used; it is not bundled.

## Validation

- `dotnet publish` Release succeeded. Two CS0067 warnings reference the existing
  unused WakePhraseRecognized event; no build errors.
- Bundled Python `pip check`: no broken requirements.
- Imported the installed backend and PDF, DOCX, Whisper, cryptography, and
  process-monitoring dependencies successfully.
- Confirmed Python runs from the bundled installation and does not reference
  the repository's virtual environment.
- In-process ASGI smoke checks: `/health` and `/api/session` returned HTTP 200.
  Session check used a temporary database and test-only authorization override
  in the validation process. No deployed authorization settings were changed.
- Verified the monitoring endpoint has one route registration.
- Verified all staged payload file sizes at the destination, SHA-256 equality
  for all 52 backend Python sources, and desktop EXE/DLL hashes.

The normal backend lifespan and desktop application were not launched during
validation: no production monitoring, email delivery, or workflow execution
was triggered. Full desktop/manual acceptance remains separate.

## Launch and Workshop validation

Launch `D:\Program Files\GDIT\Aegis\Aegis-9\Aegis.Desktop.exe`.
The desktop locates `backend\python\python.exe` automatically.
Only one Aegis backend should own localhost port 8000.

For the standalone synthetic Workshop check, use PowerShell:

```powershell
Set-Location 'D:\Program Files\GDIT\Aegis\Aegis-9'
& '.\backend\python\python.exe' '.\scripts\validate-workshop-local.py' --generate
```

This substitutes bundled Python for the repository `.venv` commands in the
copied manual test documentation. Consult `docs\WORKSHOP-MANUAL-TEST-PLAN.md`
before running the complete manual workflow process.
